const express = require("express");
const auth = require("../middleware/auth");
const router = express.Router();

// router.post("/like", auth, (req, res) => {
//   let { ownerId, commentId } = req.body;
//   const {error} =
// });

module.exports = router;
